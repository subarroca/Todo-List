describe('Model: TodoSection', () => {
  it('initialises with a title', () => {
  });

  it('is empty by default', () => {
  });

  it('contains defined items', () => {
  });

  it('flushes', () => {
  });

  it('adds items', () => {
  });

  it('removes items', () => {
  });

  it('removes nothing if item is not present', () => {
  });

  it('returns ordered items', () => {
  });
})